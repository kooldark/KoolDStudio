name: Tự động tạo danh sách ảnh portfolio

on:
  push:
    branches:
      - main # Hoặc tên nhánh chính của bạn
    paths:
      - 'assets/img/portfolio/**'

jobs:
  generate-json:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run script to generate images-list.json
        run: node scripts/generate-gallery.js

      - name: Commit and push if there are changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'chore: 🤖 Tự động cập nhật danh sách ảnh'
          file_pattern: 'assets/img/portfolio/images-list.json'
          commit_user_name: 'github-actions[bot]'
          commit_user_email: '41898282+github-actions[bot]@users.noreply.github.com'